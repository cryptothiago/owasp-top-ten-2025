- uses: actions/checkout@v4
  with:
    fetch-depth: 0   # necessário: o scanner precisa do histórico completo do git

- uses: idrix-security/scan-action@v1
  with:
    api-url: https://idrix-gateway.publivita.workers.dev
    api-token: ${{ secrets.IDRIX_CI_TOKEN }}
